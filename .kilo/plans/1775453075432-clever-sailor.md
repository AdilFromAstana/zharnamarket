# Актуализированная документация проекта: Zharnamarket PARTNER (viral-wall-frontend)

> Обновлено 2026-04-06 на основе ревизии кодовой базы. Предыдущая версия: `1775449368573-clever-island.md`.

---

## Что изменилось с момента написания оригинального плана

### РЕАЛИЗОВАНО (из Секции 9 — Роль Admin)

| Фаза | Описание | Статус |
|------|---------|--------|
| 1.1 | Enum `Role` + поле `role` в User (schema.prisma) | **Готово** |
| 1.2 | Seed admin-пользователя | **Готово** |
| 1.3 | `role` в JWT payload, `requireAdmin()`, `forbidden()` в `lib/auth.ts` | **Готово** |
| 1.4 | Все auth-эндпоинты обновлены (login, register, refresh, google callback) | **Готово** |
| 1.5 | `/api/users/me` возвращает `role` | **Готово** |
| 1.6 | Admin promo endpoints защищены через `requireAdmin()` (TODO убраны) | **Готово** |
| 2.1 | AuthContext: `role` в `AuthUser`, `isAdmin` в контексте | **Готово** |
| 2.2 | `hooks/useRequireAdmin.ts` — хук защиты админ-страниц | **Готово** |
| 2.3 | `/admin` в `PROTECTED_PATHS` в `proxy.ts` | **Готово** |
| 2.4 | Ссылка "Админ-панель" в AppHeader (desktop + mobile drawer) | **Готово** |
| 3.2 | `app/admin/page.tsx` — дашборд со статистикой | **Готово** |
| 3.3 | `app/admin/promo/page.tsx` — список промокодов | **Готово** |
| 3.4 | `app/admin/promo/new/page.tsx` — создание промокода | **Готово** |
| 3.5 | `app/admin/promo/[id]/page.tsx` — редактирование + история | **Готово** |
| 4.1 | `GET /api/admin/stats` — API статистики | **Готово** |
| 5 | Бесплатная публикация через 100%-промокоды | **Работает** (кроме `creator_publication`) |

### РЕАЛИЗОВАНО (из Секции 10 — Интеграционные тесты)

| Компонент | Статус |
|-----------|--------|
| Vitest установлен и настроен (`vitest.config.ts`) | **Готово** |
| `tests/setup.ts` — глобальная настройка | **Готово** |
| `tests/helpers/auth.ts` — хелперы | **Готово** |
| `tests/api/admin/auth.test.ts` — тесты авторизации admin | **Готово** |
| `tests/api/auth/register.test.ts` — тесты регистрации | **Готово** |
| `tests/api/auth/login.test.ts` — тесты логина | **Готово** |
| `tests/api/admin/promo-crud.test.ts` — CRUD промокодов | **Готово** |
| `tests/api/admin/stats.test.ts` — статистика | **Готово** |
| Скрипты `test` / `test:watch` в package.json | **Готово** |

---

## Текущее состояние: что ещё НЕ реализовано

### Критические / Высокий приоритет

| # | Проблема | Серьёзность | Детали |
|---|---------|-------------|--------|
| 1 | **Платёжная интеграция (Kaspi/Halyk/карта)** | CRITICAL | Все платежи симулируются как `"success"`. TODO в `app/api/payments/ads/[id]/publish/route.ts:117` и `boost/route.ts:130` |
| 2 | **Google OAuth: JWT в URL параметрах** | HIGH | `app/api/auth/google/callback/route.ts:118-131` — токены видны в логах, истории браузера, Referer |
| 3 | **Нет CSRF-защиты в OAuth** | HIGH | `app/api/auth/google/route.ts` — отсутствует `state` параметр |
| 4 | **JWT в localStorage** | HIGH | `lib/api-client.ts:18-51` — уязвимо к XSS |
| 5 | **Refresh-токены не трекаются в БД** | HIGH | Украденный токен валиден 30 дней |
| 6 | **Нет Content-Security-Policy** | HIGH | Нигде не настроено |

### Заглушки в коде (TODO / stubs)

| # | Фича | Где | Текущее состояние |
|---|------|-----|-------------------|
| 1 | **Забытый пароль** | `app/auth/forgot-password/page.tsx:16` | UI есть, API — `setTimeout()` заглушка. Эндпоинт не существует |
| 2 | **Загрузка аватара** | `app/settings/page.tsx` | `<Upload>` компонент без `action`/`customRequest`. Декоративный |
| 3 | **Удаление аккаунта** | `app/settings/page.tsx:332-345` | Backend `DELETE /api/users/me` реализован, но фронтенд вызывает только `toast.error()` без API-запроса |
| 4 | **Жалоба на объявление** | `app/ads/[id]/AdDetailClient.tsx:158` | `toast.info("Жалоба отправлена")` — заглушка, API нет |
| 5 | **Жалоба на креатора** | `app/creators/[id]/CreatorDetailClient.tsx:397` | Аналогичная заглушка |
| 6 | **Настройки уведомлений** | `app/settings/page.tsx` | API (`GET/PUT /api/settings/notifications`) реализован и работает. Но фронтенд НЕ вызывает API — кнопка "Сохранить" показывает toast без запроса, начальные данные не загружаются |
| 7 | **Другие профили креатора** | `app/creators/[id]/CreatorDetailClient.tsx:39` | `otherProfiles: CreatorProfile[] = []` — всегда пустой массив |
| 8 | **Изображения в AdCard** | `components/ads/AdCard.tsx:229-260` | Секция с картинкой **закомментирована** |
| 9 | **Оплата публикации креатора** | `PaymentType.creator_publication` | Enum определён, API-эндпоинт `/api/payments/creators/[id]/publish` не существует |

### Полностью отсутствующие функции

| # | Фича | Описание |
|---|------|---------|
| 1 | **Мессенджер/чат** | Связь только через внешние контакты (Telegram, WhatsApp). Нет моделей, API, UI |
| 2 | **Система отзывов/рейтингов** | Нет модели Review/Rating. Нет API. Нет UI |
| 3 | **Push/Email уведомления** | Модель `NotificationSettings` есть. Отправка не реализована. Нет интеграции с email/push сервисом |
| 4 | **Модерация контента (UI)** | Модель `Report` в схеме есть. API для создания Report — нет. UI админки для модерации — нет |
| 5 | **Загрузка файлов** | Нет ни одного upload-эндпоинта. Изображения хранятся как URL-строки |
| 6 | **Полнотекстовый поиск** | Нет поиска по тексту объявлений/креаторов. Только фильтры (город, платформа, категория) |
| 7 | **Email-верификация** | Пользователь сразу активен после регистрации. Поле `verified` в профилях — ручное |
| 8 | **Аналитика для пользователей** | Нет панели аналитики для рекламодателей/креаторов |
| 9 | **Система подписок** | Нет recurring payments |
| 10 | **CI/CD** | Нет `.github/workflows/`, `.gitlab-ci.yml`, Jenkinsfile |

### Валидация и безопасность

| # | Проблема | Серьёзность |
|---|---------|-------------|
| 1 | **Zod установлен но НЕ используется** — 0 импортов. Вся валидация через `if` | MEDIUM |
| 2 | **Нет серверной валидации email-формата** — только client-side через Ant Design | MEDIUM |
| 3 | **Нет валидации макс. длины текстовых полей** — описания, имена, bio без ограничений | MEDIUM |
| 4 | **Нет валидации URL** в полях images[], website, videoUrl | MEDIUM |
| 5 | **Нет санитизации пользовательского ввода** — нет DOMPurify или аналога | MEDIUM |
| 6 | **Счётчик просмотров легко накручивается** — нет rate limit, нет дедупликации по IP/user | MEDIUM |
| 7 | **Счётчик кликов на контакты** — публичный эндпоинт без защиты | MEDIUM |
| 8 | **Промокоды можно подбирать перебором** — `GET /api/promo/validate` без rate limit | MEDIUM |
| 9 | **Rate limit только in-memory** — сбрасывается при рестарте, не работает в кластере | MEDIUM |
| 10 | **Dev JWT secret в fallback** — `lib/auth.ts:14`: `"dev-fallback-secret-do-not-use-in-production"` | LOW |
| 11 | **Нет структурированного логирования** — только `console.error` | LOW |
| 12 | **Нет мониторинга ошибок** (Sentry, DataDog) | LOW |

---

## Сводная таблица статусов

| Секция оригинального плана | Статус |
|---------------------------|--------|
| §1 Продукт (описание) | Актуально, без изменений |
| §2 Роли и пользователи | **Обновлено**: Admin роль теперь реализована. Enum `Role` в schema, `role` поле в User, `requireAdmin()` в auth. Остальное актуально |
| §3 Монетизация | Актуально. Платежи по-прежнему заглушки |
| §4 Ключевые фичи | **Обновлено**: добавлены реализованные admin-фичи. Список "не доделано" уменьшился (см. выше) |
| §5 Технический стек | **Обновлено**: Vitest добавлен в dev-зависимости. Zustand по-прежнему не используется. Zod по-прежнему не используется |
| §6 Структура БД | **Обновлено**: добавлен enum `Role` и поле `role` в User |
| §7 API | **Обновлено**: добавлен `GET /api/admin/stats`. Admin promo эндпоинты теперь защищены |
| §8 Слабые места | **Обновлено**: проблема #1 (Admin API без проверки роли) — ИСПРАВЛЕНА. Остальные актуальны |
| §9 План: Роль Admin | **ПОЛНОСТЬЮ РЕАЛИЗОВАНО**. Все 5 фаз, все 19 шагов выполнены |
| §10 План: Интеграционные тесты | **ПОЛНОСТЬЮ РЕАЛИЗОВАНО**. Vitest настроен, ~48 тест-кейсов написаны |

---

## Что можно реализовать дальше (приоритезированные предложения)

### Приоритет 1 — Быстрые исправления (заглушки → рабочий код)

1. **Подключить настройки уведомлений к API** — фронтенд `app/settings/page.tsx` вызывает `GET/PUT /api/settings/notifications` (API уже работает, нужно ~20 строк кода)
2. **Подключить удаление аккаунта** — `handleDeleteAccount` → `api.delete("/api/users/me")` (backend уже работает)
3. **Раскомментировать изображения в AdCard** — `components/ads/AdCard.tsx:229-260`
4. **Загрузить otherProfiles в CreatorDetailClient** — сделать API-запрос по `userId`

### Приоритет 2 — Безопасность

5. **CSRF state в OAuth** — добавить `state` параметр в Google OAuth flow
6. **Перенести JWT в httpOnly cookie** — убрать из localStorage
7. **Добавить CSP заголовки** — через `next.config.ts` headers
8. **Внедрить Zod-валидацию** на серверные эндпоинты (уже установлен, 0 использований)
9. **Добавить rate limit на `/api/promo/validate`**

### Приоритет 3 — Новые функции

10. **Забытый пароль** — `POST /api/auth/forgot-password` + email интеграция
11. **Загрузка файлов** — upload endpoint + S3/Cloudinary интеграция
12. **Жалобы/Report API** — `POST /api/reports` (модель уже в schema)
13. **Платёжная интеграция** — Kaspi/Halyk SDK
14. **Полнотекстовый поиск** — по тексту объявлений/креаторов
15. **CI/CD** — GitHub Actions pipeline (test → build → deploy)
