# Plan: Escrow UX — разбивка суммы, кнопка, убрать 7 дней

## Файл: `app/ads/new/page.tsx`

---

### 1. Кнопка step 4 — неверная сумма для escrow

`nextLabel` для step 4 всегда использует `finalPayPrice = PUBLICATION_PRICE = 990 ₸`.
Для escrow нужно показывать `totalBudget` и другой глагол («Заморозить», не «Оплатить»).

```tsx
// До (~line 821):
: `Оплатить ${formatPrice(finalPayPrice)}`

// После:
: isEscrowMode
    ? `Заморозить ${formatPrice(form.getFieldValue("totalBudget") ?? 0)}`
    : `Оплатить ${formatPrice(finalPayPrice)}`
```

---

### 2. Разбивка суммы в escrow-карточке (step 4, ~lines 1699–1726)

Заменить текущий скромный блок с одной суммой на наглядную разбивку:

```
Бюджет задания          50 000 ₸
Комиссия (5% с выплат)  ~ 2 500 ₸   ← серый, курсив
────────────────────────────────────
Замораживается сейчас   50 000 ₸   ← жирный
```

Пояснение под таблицей (маленький текст):
> «Комиссия 5% вычитается из каждой выплаты креатору — не из вашего бюджета»

Приблизительная комиссия вычисляется как `totalBudget * PLATFORM_COMMISSION_RATE`.

**Конкретная JSX-замена** блока `<div className="space-y-3 mb-4">` (~lines 1701–1726):

```tsx
<div className="mb-4 space-y-3">
  {/* Разбивка */}
  <div className="rounded-xl border border-green-200 overflow-hidden">
    <div className="flex justify-between items-center px-4 py-3 bg-white">
      <span className="text-sm text-gray-600">Бюджет задания</span>
      <span className="font-semibold text-gray-900">
        {formatPrice(totalBudget)}
      </span>
    </div>
    <div className="flex justify-between items-center px-4 py-3 bg-white border-t border-gray-100">
      <span className="text-sm text-gray-400 italic">
        Комиссия платформы ({PLATFORM_COMMISSION_RATE * 100}% с выплат)
      </span>
      <span className="text-sm text-gray-400 italic">
        ~ {formatPrice(Math.round(totalBudget * PLATFORM_COMMISSION_RATE))}
      </span>
    </div>
    <div className="flex justify-between items-center px-4 py-3 bg-green-50 border-t border-green-200">
      <span className="text-sm font-semibold text-gray-900">
        Замораживается сейчас
      </span>
      <span className="text-xl font-bold text-gray-900">
        {formatPrice(totalBudget)}
      </span>
    </div>
  </div>
  <p className="text-xs text-gray-400 px-1">
    Комиссия вычитается из выплат креаторам — не списывается с вашего бюджета отдельно.
    RPM: {form.getFieldValue("rpm")} ₸ / 1 000 просм. · Остаток можно вернуть.
  </p>
</div>
```

Вместо `totalBudget` использовать `form.getFieldValue("totalBudget") ?? 0`.

---

### 3. «К оплате» total-строка для escrow (~lines 1806–1820)

Сейчас:
```tsx
<span className="font-bold text-gray-900">
  {isEscrowMode
    ? formatPrice(form.getFieldValue("totalBudget") ?? 0)
    : formatPrice(finalPayPrice)}
</span>
```

Для escrow заменить на полный блок с подписью:
```tsx
{isEscrowMode ? (
  <div className="text-right">
    <div className="text-2xl font-bold text-gray-900">
      {formatPrice(form.getFieldValue("totalBudget") ?? 0)}
    </div>
    <div className="text-xs text-gray-400 mt-0.5">
      + ~{formatPrice(Math.round((form.getFieldValue("totalBudget") ?? 0) * PLATFORM_COMMISSION_RATE))} комиссия с выплат
    </div>
  </div>
) : (
  <span className="font-bold text-gray-900">
    {formatPrice(finalPayPrice)}
  </span>
)}
```

---

### 4. Убрать «7 дней» для escrow

**A. Step 3 preview banner (~line 1680–1683)**

```tsx
// До:
<div className="bg-gray-50 rounded-xl px-4 py-3 text-sm text-gray-600">
  После оплаты объявление пройдёт модерацию и появится в ленте
  на <b>{PUBLICATION_DAYS} дней</b>.
</div>

// После:
<div className="bg-gray-50 rounded-xl px-4 py-3 text-sm text-gray-600">
  {isEscrowMode ? (
    <>После публикации задание будет активно <b>бессрочно</b> — пока бюджет не исчерпан или вы не закроете его вручную.</>
  ) : (
    <>После оплаты объявление пройдёт модерацию и появится в ленте на <b>{PUBLICATION_DAYS} дней</b>.</>
  )}
</div>
```

**B. StepSidebar — «Итого к оплате» для escrow (~line 447)**

```tsx
// До:
<p className="text-xs text-gray-500 mt-1">Публикация на 7 дней</p>

// После:
<p className="text-xs text-gray-500 mt-1">
  {isEscrowMode ? "Бюджет замораживается бессрочно" : "Публикация на 7 дней"}
</p>
```

---

## Итого — все затронутые строки

| Строка | Изменение |
|--------|-----------|
| ~821 | `nextLabel` step 4: escrow → «Заморозить X ₸» |
| ~1701–1726 | Escrow-карточка: разбивка бюджет + ~комиссия + итого |
| ~1806–1820 | «К оплате» total: для escrow добавить строку с ~комиссией |
| ~1680–1683 | Step 3 preview: для escrow «бессрочно», для прямого «7 дней» |
| ~447 | StepSidebar step 4: для escrow «бессрочно» вместо «7 дней» |

---

# Plan: Три исправления

## Задача 1 — Комиссия платформы: 20% → 5%

### Контекст
- `lib/constants.ts` строка 255: `export const PLATFORM_COMMISSION_RATE = 0.20;`
- Это единственная каноническая константа, но "20%" захардкожено в двух местах UI

### Изменения

**A. `lib/constants.ts` (~line 255)**
```ts
// До:
export const PLATFORM_COMMISSION_RATE = 0.20; // 20%
// После:
export const PLATFORM_COMMISSION_RATE = 0.05; // 5%
```

**B. `app/ads/new/page.tsx` (~line 1712)**  
Убрать захардкоженный текст, заменить динамическим значением из константы:
```tsx
// До:
<p>Комиссия платформы: 20% с каждой выплаты</p>
// После:
<p>Комиссия платформы: {PLATFORM_COMMISSION_RATE * 100}% с каждой выплаты</p>
```
Добавить `PLATFORM_COMMISSION_RATE` в импорт из `@/lib/constants`.

**C. `app/tasks/[id]/submit/page.tsx` (~line 160)**
```tsx
// До:
<span>Комиссия платформы (20%)</span>
// После:
<span>Комиссия платформы ({PLATFORM_COMMISSION_RATE * 100}%)</span>
```
`PLATFORM_COMMISSION_RATE` уже используется в вычислениях (строки 27-28), но не в тексте — только добавить в текстовый лейбл.

**D. `app/balance/withdraw/page.tsx`**  
Добавить информационную строку перед кнопкой «Запросить вывод»:
```tsx
<p className="text-xs text-gray-400 text-center">
  Комиссия платформы: {PLATFORM_COMMISSION_RATE * 100}% от суммы вывода
</p>
```
Импортировать `PLATFORM_COMMISSION_RATE` из `@/lib/constants`.

---

## Задача 2 — Баланс кошелька в личном кабинете

### Где проблема
`/app/cabinet/page.tsx` (525 строк) не показывает текущий баланс и не имеет ссылок на `/balance`.  
Страница `/balance/page.tsx` уже полностью реализована, просто до неё нельзя добраться из кабинета.

### Изменения

**A. `app/cabinet/page.tsx`**

1. Добавить `walletBalance` state и загрузить баланс вместе с остальными данными:
```ts
const [walletBalance, setWalletBalance] = useState<number | null>(null);
// В Promise.all добавить:
api.get<{ balance: { current: number } }>("/api/balance").catch(() => ({ balance: { current: 0 } }))
// Из результата:
setWalletBalance(walletBalanceData.balance.current);
```

2. Добавить компактный виджет баланса **перед блоком «Быстрые действия»** (~line 475):
```tsx
{/* Кошелёк */}
<div className="bg-white rounded-2xl border border-gray-200 p-5 mb-4">
  <div className="flex items-center justify-between">
    <div>
      <p className="text-xs text-gray-400 font-medium uppercase tracking-wider mb-1">
        Баланс кошелька
      </p>
      <p className="text-2xl font-bold text-gray-900">
        {walletBalance === null ? "—" : walletBalance.toLocaleString("ru")} ₸
      </p>
    </div>
    <div className="flex gap-2">
      <Link href="/balance/topup">
        <Button size="small" type="primary">Пополнить</Button>
      </Link>
      <Link href="/balance">
        <Button size="small">Операции</Button>
      </Link>
    </div>
  </div>
</div>
```

3. Добавить `WalletOutlined` в импорты `@ant-design/icons` (или использовать `CreditCardOutlined`, уже импортированный).

**B. `components/layout/AppHeader.tsx`**

1. Добавить «Кошелёк» в `accountMenuItems` (~line 122, после «Мой кабинет»):
```ts
{
  key: "balance",
  icon: <WalletOutlined />,
  label: <Link href="/balance">Кошелёк</Link>,
},
```
Добавить `WalletOutlined` в импорты `@ant-design/icons`.

2. Добавить «Кошелёк» в мобильный drawer (~line 319, после «Кабинет»):
```tsx
{isLoggedIn && (
  <Link href="/balance" onClick={() => setDrawerOpen(false)} className={cn(...)}>
    <span className="text-base"><WalletOutlined /></span>
    Кошелёк
  </Link>
)}
```

---

## Задача 3 — Описание в превью шага 3 рендерит сырой HTML

### Где проблема
`app/ads/new/page.tsx` ~строки 1643–1647:
```tsx
{form.getFieldValue("description") && (
  <div className="text-sm text-gray-700 leading-relaxed mb-3 whitespace-pre-line line-clamp-4">
    {form.getFieldValue("description")}   ← рендерит "<h2>...</h2>" как текст
  </div>
)}
```

### Как AdDetailClient делает правильно (строки 394–400)
```tsx
{ad.description.includes("<") ? (
  <RichTextContent html={ad.description} />
) : (
  <p className="text-gray-700 leading-relaxed whitespace-pre-wrap text-sm">
    {ad.description}
  </p>
)}
```

### Изменения в `app/ads/new/page.tsx`

1. Добавить импорт (уже есть `RichTextEditor`, добавить рядом):
```tsx
import RichTextContent from "@/components/ui/RichTextContent";
```

2. Заменить строки 1643–1647:
```tsx
{form.getFieldValue("description") && (
  (() => {
    const desc = form.getFieldValue("description") as string;
    return desc.includes("<") ? (
      <div className="mb-3 line-clamp-4 text-sm [&_*]:leading-relaxed">
        <RichTextContent html={desc} />
      </div>
    ) : (
      <div className="text-sm text-gray-700 leading-relaxed mb-3 whitespace-pre-line line-clamp-4">
        {desc}
      </div>
    );
  })()
)}
```

---

## Затронутые файлы

| Файл | Задача |
|------|--------|
| `lib/constants.ts` | 1 — ставка 0.20 → 0.05 |
| `app/ads/new/page.tsx` | 1 — убрать хардкод "20%"; 3 — фикс описания |
| `app/tasks/[id]/submit/page.tsx` | 1 — убрать хардкод "20%" |
| `app/balance/withdraw/page.tsx` | 1 — добавить информ. строку о комиссии |
| `app/cabinet/page.tsx` | 2 — виджет баланса |
| `components/layout/AppHeader.tsx` | 2 — ссылка «Кошелёк» в dropdown + drawer |
